on:
  push:
  pull_request:
  workflow_dispatch:
  workflow_run:
    workflows: ["Update Nice!View Images"]
    types: [completed]

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@v0.3
